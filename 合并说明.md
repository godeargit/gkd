# GKD 规则合并工具 — 使用说明

把本目录下多份 GKD 订阅规则合并为 1 份, 并自动去除重复规则。
输出文件符合 GKD App 的订阅格式(JSON5), 可直接导入 App 使用。

## 一键运行(推荐)

双击 **`合并规则.bat`**, 等待窗口显示报告后按任意键关闭。
生成的成果文件:

| 文件 | 说明 |
| --- | --- |
| `merged_gkd.json5` | 合并后的订阅规则(导入 App 用这个) |
| `merged_gkd.version.json5` | 配套版本文件(订阅更新检查用) |
| `merged_gkd_report.txt` | 合并统计报告 |

## 命令行运行

需要 Node.js (>= 12): <https://nodejs.org/>

```bat
cd /d E:\GKD
node merge_gkd.cjs                 :: 合并并生成文件
node merge_gkd.cjs --dry-run       :: 只预览报告, 不写文件
node merge_gkd.cjs --out my.json5  :: 自定义输出文件名
node merge_gkd.cjs --id 9527 --name "我的订阅" --author "me"
```

## 合并了哪些文件

| 文件 | 订阅 | 说明 |
| --- | --- | --- |
| `gkd/subscription/667.json` | id667 的 GKD 订阅 | App 本地保存版(v592) |
| `gkd/subscription/666.json` | AIsouler 的 GKD 订阅 | App 本地保存版(v406) |
| `gkd3.json5` | id667 的 GKD 订阅 | 上游文件(v590, 与上面同 id, 自动取高版本) |
| `AIsouler_gkd.json5` | AIsouler 的 GKD 订阅 | 上游文件(v406) |
| `Adpro_gkd.json5` | Adpro 的 GKD 订阅 | |
| `gkd.json5` | 梦念逍遥のGKD订阅 | |
| `aoguai_gkd.json5` | 奥怪的 GKD 订阅 | |
| `ganlin_gkd.json5` | 甘霖的 GKD 订阅 | |

> 同一订阅 id 存在多份文件时, 程序自动采用 version 最高的那份, 无需手动删减。

**不参与合并的文件:**
- `gkd2.json5` — 是下载失败的 GitHub HTML 页面, 不是规则文件(程序会自动识别跳过)
- `gkd/subscription/-2.json` — App 内置"本地订阅", 当前为空
- `bak/` 目录下的 zip — 备份压缩包

## 去重/合并规则

1. **应用**按包名合并: 多个订阅中同一个 App 的规则全部合并到一起。
2. **规则组**去重: 组名 + 规则内容完全一致才判定为重复并去除;
   同名的不同规则组会**保留**(它们通常覆盖不同的广告场景, 去掉会丢功能)。
3. **规则**去重: 匹配内容完全一致的去重, 快照 URL(snapshotUrls)取并集。
4. **全局规则组**: "开屏广告" 与 "开屏广告-全局" 语义相同, 合并为 1 个组,
   规则取并集去重; 组属性采用主流配置(fastQuery / matchTime=10000 / actionMaximum=2)。
5. **key 重新编号**: 全局组、应用组、组内规则全部重新编号,
   并自动修正 `actionCdKey` / `actionMaximumKey` / `preKeys` 引用,
   保证格式合法, App 可正常导入。
6. **分类**统一为 GKD App 默认分类(0~10)。

## 附加规则: 支付弹窗-全局(新增)

合并后的文件额外注入了一个**全局规则组「支付弹窗-全局」**, 用于屏蔽
App 动不动弹出的支付/会员/充值/购买弹窗(如"开通会员""立即购买""去支付")。

**工作方式**(选择器语法已对照 GKD 官方文档核实):

| 策略 | 说明 |
| --- | --- |
| 取消类按钮 + 支付按钮兄弟 | 找到 `取消/暂不/以后再说/残忍拒绝/不需要` 类按钮, 若其前方或后方相邻兄弟是 `立即开通/开通会员/去支付/立即购买` 等支付类按钮, 则点击该否定按钮 |
| 取消类按钮 + 弹窗内支付文案 | 否定按钮所在的弹窗(任意祖先节点)内含有 `支付/充值/会员/续费/付费` 文案时, 点击该按钮 |
| 关闭按钮 + 弹窗内支付文案 | 按 `关闭/✕/×` 文字或 `close/dismiss` 等 id 找到关闭按钮, 且弹窗内有支付类文案时点击 |

**安全设计**(不会误点支付!):

- 只点击 **否定类** 按钮(`取消/暂不/关闭` 等), 支付类按钮(含"支付/购买/开通/充值"字样的节点)
  只作为判断上下文,**永远不会被点击**;
- 出现 `支付密码/交易密码/验证指纹/安全验证` 等**真实支付校验界面**时不执行任何操作
  (避免干扰真实付款流程);
- 每个 App 每次启动最多点击 2 次(`actionMaximum: 2`), 切换 App 后重置;
- 若某个 App 自带名为「支付弹窗」的规则组, 全局规则对该 App 自动失效
  (`disableIfAppGroupMatch`)。

**如何停用/调整**:
- 编辑 `merge_gkd.cjs` 顶部 `EXTRA_GLOBAL_GROUPS`, 置为 `[]` 即不注入;
- 或导入后在 GKD App 的订阅详情里把「支付弹窗-全局」组停用;
- 若某个 App 不需要该全局规则, 可在该组的 `apps` 列表里添加
  `{id: "包名", enable: false}`。

## 导入到 GKD App

1. 把 `merged_gkd.json5` 传到手机(或用手机浏览器下载)。
2. 打开 GKD → 订阅 → 右上角"+" → 从本地文件导入 → 选择该文件。
3. 如需订阅更新检查, 把 `merged_gkd.version.json5` 与规则文件一起托管到
   任意静态服务器/GitHub, 并把 `checkUpdateUrl` 改为对应 URL。

## 更新离线备份 zip

把合并结果替换进 `bak\backup(离线规则).zip`(保持 zip 内部结构, 原文件自动备份为 `.bak`):

```bat
cd /d E:\GKD
node update_backup_zip.cjs --dry-run   :: 先查看 zip 内部文件与替换计划(不改动)
node update_backup_zip.cjs              :: 执行替换
```

脚本会自动(以 GKD 导出备份格式为准):
- 找出 zip 内 `subscription/` 目录下的订阅规则文件(取最大的, 如 `subscription/667.json`), 内容替换为 `merged_gkd.json5`;
- 将条目按新内容 id 重命名(如 `subscription/667.json` -> `subscription/9527.json`, 与 GKD 保存订阅的命名规则一致);
- 同步更新 `db.json` 中 `subsItems` 的订阅 id(667 -> 9527)并清空 `updateUrl`, 保证恢复备份后订阅状态一致;
- 其余文件(`store/` 等)原样保留, 重新打包并自校验回读。

如需更新其他备份(如 `bak\gkd-backup-*.zip`), 把 zip 路径作为参数传入即可。

## 修改源列表

编辑 `merge_gkd.cjs` 顶部的 `SOURCES` 数组即可增删参与合并的文件,
不需要改其它代码。
